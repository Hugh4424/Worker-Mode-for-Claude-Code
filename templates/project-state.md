# Project State — {项目名}

> **使用说明（FR-STATE-003 防假委派原则）**
>
> 主会话（orchestrator）只读这份轻量状态文件做调度判断，不加载任何重内容文件。
> Worker 凭这份文件提供的路径地图**自取**上下文；重内容只进 worker，**从不进主会话**。
> 这从根上堵死"主会话先全读重内容再打包给 worker"的假委派模式。

---

## 1. 项目基本信息

| 字段 | 值 |
|------|-----|
| 项目名 | `{项目名}` |
| 目标 | `{一句话描述核心交付目标}` |
| 仓库根路径 | `{绝对路径或相对路径}` |
| 主要语言/框架 | `{语言, 框架}` |

---

## 2. 当前阶段与进度

**当前阶段：** `{阶段名，如 design / apply / review / close}`

**阶段目标：** `{本阶段要完成什么}`

**进度概览：**
- [x] `{已完成步骤1}`
- [ ] `{进行中步骤2}`
- [ ] `{待做步骤3}`

**已知阻塞：**
- `{阻塞项描述，如无填"无"}` 

---

## 3. 关键文件与路径地图

> Worker 凭此地图被指派后自取对应文件，主会话不预先读取这些文件内容。

| 用途 | 路径 |
|------|------|
| 核心业务逻辑 | `<关键源码路径>` |
| 测试文件 | `<测试目录路径>` |
| 配置文件 | `<配置文件路径>` |
| 规范/spec 文档 | `<spec文档路径>` |
| 接口定义 | `<接口定义路径>` |
| 其他关键文件 | `<其他关键文件路径>` |

---

## 4. 待办与优先级

按优先级从高到低列出当前待办（主会话用此做调度判断）：

1. `{待办1 — 优先级：高}`
2. `{待办2 — 优先级：中}`
3. `{待办3 — 优先级：低}`

---

## 5. 约定与决策指针

> 指向具体决策记录，worker 需要时自取，不在此展开细节。

| 决策/约定 | 文件/位置 |
|-----------|-----------|
| `{约定名称，如：命名规范}` | `{path/to/decision-log.md#anchor}` |
| `{约定名称，如：测试策略}` | `{path/to/spec.md#section}` |
| `{约定名称，如：API 风格}` | `{path/to/conventions.md}` |

---

## 6. 上次更新

| 字段 | 值 |
|------|-----|
| 更新时间 | `{YYYY-MM-DD HH:MM}` |
| 更新人/触发来源 | `{人工 / 自动 / worker: implementer}` |
| 变更摘要 | `{一句话说明本次更新了什么}` |
